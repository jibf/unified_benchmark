# AgentHard Benchmark Suite Requirements
# Based on current working environment (agenthard conda env)
# Minimal requirements - benchmark packages will install their own dependencies

# Core packages that may not be installed by benchmark dependencies
openai==1.100.2
datasets==4.0.0
dill==0.3.8
huggingface-hub==0.34.4
multiprocess==0.70.16
numpy==1.26.4
overrides==7.7.0
pandas==2.3.1
pyarrow==21.0.0
tenacity==9.1.2
tree-sitter==0.21.3
tree-sitter-java==0.21.0
tree-sitter-javascript==0.21.4
python-dotenv
FlagEmbedding
anthropic==0.53.0
torchvision==0.20.1
wcwidth==0.2.13
openpyxl==3.1.5

# Packages that may be needed for some benchmarks
# (Uncomment if missing after benchmark package installation)
# google-generativeai==0.8.3
# mistralai==1.9.1
# cohere==5.0.0
# torch>=2.0.0
# transformers>=4.0.0
# sentence-transformers
# litellm
# tiktoken
# redis
# scikit-learn
# matplotlib
# plotly
# fastapi
# uvicorn
# pytest